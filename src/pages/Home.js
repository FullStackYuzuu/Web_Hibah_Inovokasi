import React from 'react';
import Hero from '../components/Hero';
import Content from '../components/ContentHome';

const Home = ({}) => {
  return (
    <div>
      <Hero />
      <Content/>
    </div>
  );
};

export default Home;
